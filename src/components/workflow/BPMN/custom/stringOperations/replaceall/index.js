import Replaceallpalette from './replaceAllPalette';

const ReplaceallOperation = {
  __init__: ['replaceallPalette'],
  replaceallPalette: ['type', Replaceallpalette]
};

export default ReplaceallOperation;
